export interface Project {
    id: number,
    title: string,
    description: string,
    github: string
}

export interface Blog {
    id: number,
    title: string,
    desription: string,
    category: string[]
}